{
  "name": "skillwork-support-chat",
  "private": true,
  "scripts": {
    "build:deploy": "node scripts/build-deploy.mjs",
    "start": "npx --yes serve . -l 5174",
    "start:user": "npm run build:deploy && npx --yes serve deploy-user -l 5175",
    "start:admin": "npm run build:deploy && npx --yes serve deploy-admin -l 5176",
    "dev": "npm run start"
  }
}
